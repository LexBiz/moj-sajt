# 🚀 Premium Developer Portfolio Website

Современный премиум одностраничник для продажи услуг разработки и автоматизации бизнеса.

## ✨ Что внутри

### Дизайн уровня топ-продакшна
- 🎨 **Градиентные анимации** — живые переходы цветов, floating элементы
- 💎 **Glassmorphism эффекты** — полупрозрачные карточки с blur
- 🌊 **Scroll-анимации** — элементы плавно появляются при прокрутке
- 🎯 **Премиум палитра** — cyan/purple градиенты + акценты
- ⚡ **Hover-эффекты** — scale, shadow, translate на всех интерактивных элементах
- 📱 **Полный адаптив** — идеально на всех устройствах

### Технологии
- **Next.js 14** с App Router
- **TypeScript**
- **Tailwind CSS** с кастомными анимациями
- **Inter Font** (Google Fonts)
- **IntersectionObserver API** для scroll-анимаций

## 🎯 Ключевые фичи

### Hero секция
- Full-screen с animated gradient background
- Floating элементы с разными задержками
- Glassmorphism badge
- Градиентный заголовок с text-transparent
- 2 CTA кнопки (основная + вторичная)

### Секции с контентом
- **Problem** — тёмная секция с glass-dark карточками
- **Approach** — светлая с hover-эффектами и градиентами
- **Services** — премиум карточки с "Популярно" бейджами, иконками, hover shadows
- **Trust** — glass карточки с иконками
- **Process** — нумерованные шаги с цветными градиентами
- **Contact** — full CTA секция с floating элементами

### Эффекты и анимации
- Плавное появление при скролле (`animate-on-scroll`)
- Floating элементы на фоне
- Градиентные анимации
- Scale + translate на hover
- Shadow эффекты

## 🚀 Быстрый старт

### 1. Установка
```bash
npm install
```

### 2. Замените контакты
Откройте `app/page.tsx` и найдите:
- `you@example.com` → ваш email
- `your_username` → ваш Telegram username

### 3. Запуск
```bash
npm run dev
```

Откройте **http://localhost:3000**

### 4. Production сборка
```bash
npm run build
npm start
```

## 🎨 Кастомизация

### Цветовая палитра
`tailwind.config.js`:
```js
colors: {
  primary: { ... },  // Синие тона
  accent: { ... }    // Фиолетовые тона
}
```

### Анимации
`tailwind.config.js` → `keyframes`:
- `fadeIn`, `slideUp`, `slideDown`
- `scaleIn`, `float`, `gradient`

### Glassmorphism
`app/globals.css`:
```css
.glass        // светлый glass
.glass-dark   // тёмный glass
```

## 📦 Деплой

### Vercel (рекомендуется)
```bash
npm i -g vercel
vercel
```

Или через GitHub:
1. Push код в репозиторий
2. Подключите в [vercel.com](https://vercel.com)
3. Автоматический деплой при каждом push

### Другие платформы
- Netlify
- Railway
- DigitalOcean App Platform
- AWS Amplify

## 📁 Структура

```
├── app/
│   ├── layout.tsx      # Layout + SEO + шрифты
│   ├── page.tsx        # Главная страница (client component)
│   └── globals.css     # Tailwind + кастомные классы
├── public/             # Статика
├── tailwind.config.js  # Палитра + анимации
├── package.json
└── next.config.js
```

## 🎯 Что сделать после установки

1. ✅ Замените контакты (email, Telegram)
2. ✅ Добавьте favicon в `app/`
3. ✅ Обновите SEO метаданные в `app/layout.tsx`
4. ✅ (Опционально) Настройте цвета в `tailwind.config.js`
5. ✅ Добавьте Google Analytics/Yandex Metrika

## 🔥 Фичи дизайна

- 🎨 Animated gradient backgrounds
- 💎 Glassmorphism карточки
- 🌊 Scroll-triggered анимации
- ⚡ Hover эффекты на всех элементах
- 🎯 Floating декоративные элементы
- 🚀 Smooth transitions (0.3s duration)
- 📱 Mobile-first responsive
- ♿ Семантический HTML
- 🔍 SEO оптимизация

---

**Готово к запуску!** 🚀 Замените контакты и деплойте.
