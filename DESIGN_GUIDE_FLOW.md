# Візуальний дизайн-гайд для /flow
## Система прийому заявок — детальний гайд

---

## 1. PALETTE (Палітра кольорів)

### Основні кольори
- **Primary (основний акцент)**: `#6366F1` (індіго), `#8B5CF6` (фіолетовий для градієнтів)
- **Secondary (другорядний)**: `#A78BFA` (світло-фіолетовий), `#C4B5FD` (пастельний акцент)

### Background (фон)
- **Main background**: `#0F172A` (темний slate)
- **Surface light**: `#1E293B` (картка середньої темності)
- **Surface dark**: `#0A0F1E` (максимально темний для контрасту)

### Borders (межі)
- **Default**: `rgba(255, 255, 255, 0.08)` (тонкі прозорі лінії)
- **Active**: `rgba(99, 102, 241, 0.6)` (індіго 60%)
- **Hover**: `rgba(99, 102, 241, 0.3)` (індіго 30%)

### Стани
- **Success**: `#10B981` (зелений)
- **Warning**: `#F59E0B` (жовто-помаранчевий)
- **Error**: `#EF4444` (червоний)
- **Info**: `#3B82F6` (синій)

### Текст
- **Primary text**: `#F8FAFC` (білий з легким холодним відтінком)
- **Secondary text**: `#CBD5E1` (сірий світлий)
- **Tertiary text**: `#64748B` (сірий темний для підписів)

---

## 2. TYPOGRAPHY (Типографіка)

### Шрифт
- **Основний**: Inter (Google Fonts), fallback: system-ui, -apple-system, "Segoe UI", sans-serif
- **Альтернатива (якщо треба акцент)**: Space Grotesk для заголовків

### Розміри і вага

**H1 (Головний заголовок)**
- Desktop: `42px` / `600–700` (semibold–bold)
- Mobile: `28px` / `600`
- Line-height: `1.2`
- Letter-spacing: `-0.02em`
- Використання: вступний екран

**H2 (Заголовки кроків)**
- Desktop: `32px` / `600`
- Mobile: `24px` / `600`
- Line-height: `1.3`
- Letter-spacing: `-0.01em`
- Використання: питання на кожному кроці

**H3 (Підзаголовки)**
- Desktop: `20px` / `500–600`
- Mobile: `18px` / `500`
- Line-height: `1.4`
- Використання: підписи, назви секцій у картках

**Body (основний текст)**
- Desktop: `16px` / `400`
- Mobile: `15px` / `400`
- Line-height: `1.6`
- Використання: описи, підказки

**Buttons (текст кнопок)**
- Desktop: `16px` / `600`
- Mobile: `15px` / `600`
- Використання: всі кнопки

**Captions (дрібні підписи)**
- Desktop: `14px` / `400`
- Mobile: `13px` / `400`
- Line-height: `1.5`
- Колір: tertiary text (`#64748B`)
- Використання: підказки під полями, коротка інформація

---

## 3. COMPONENTS (Компоненти)

### 3.1 Кнопки

**Primary (основна дія)**
- Background: gradient `linear-gradient(135deg, #6366F1, #8B5CF6)`
- Padding: `16px 32px` (desktop), `14px 28px` (mobile)
- Border-radius: `12px`
- Font: `16px / 600`
- Color: `#FFFFFF`
- Shadow: `0 8px 24px rgba(99, 102, 241, 0.25)`
- Hover: збільшення яскравості градієнта на 10%, shadow збільшується до `0 12px 32px rgba(99, 102, 241, 0.35)`, легкий scale `1.02`
- Active: scale `0.98`, shadow зменшується
- Disabled: opacity `0.5`, cursor `not-allowed`
- Transition: `all 0.2s ease`

**Secondary (другорядна дія)**
- Background: `rgba(255, 255, 255, 0.05)`
- Border: `1px solid rgba(255, 255, 255, 0.1)`
- Padding: `16px 32px`
- Border-radius: `12px`
- Font: `16px / 600`
- Color: `#F8FAFC`
- Hover: background `rgba(255, 255, 255, 0.1)`, border стає яскравішим
- Transition: `all 0.2s ease`

**Ghost (текстова кнопка)**
- Background: transparent
- Padding: `12px 20px`
- Border-radius: `8px`
- Font: `15px / 500`
- Color: `#A78BFA`
- Hover: background `rgba(167, 139, 250, 0.1)`
- Transition: `all 0.15s ease`

**Кнопки вибору (на кроках бізнес/канал/біль)**
- Background: `rgba(255, 255, 255, 0.05)`
- Border: `1px solid rgba(255, 255, 255, 0.1)`
- Padding: `18px 20px`
- Border-radius: `16px`
- Min-height: `64px` (для зручного тапу на мобільному)
- Font: `16px / 600`
- Color: `#F8FAFC`
- Display: flex, justify-between, align-center
- Icon праворуч: `→` (неактивна) або `✅` (вибрана)
- Hover: border `rgba(99, 102, 241, 0.3)`, background `rgba(99, 102, 241, 0.05)`
- Active (вибрана): border `rgba(99, 102, 241, 0.6)`, background `rgba(99, 102, 241, 0.1)`, shadow `0 4px 16px rgba(99, 102, 241, 0.2)`
- Transition: `all 0.2s ease`

### 3.2 Картки (Cards)

**Основна картка контенту**
- Background: `rgba(30, 41, 59, 0.4)` з backdrop-blur `12px`
- Border: `1px solid rgba(255, 255, 255, 0.08)`
- Border-radius: `24px`
- Padding: `32px` (desktop), `24px` (mobile)
- Shadow: `0 8px 32px rgba(0, 0, 0, 0.3)`
- Max-width: `640px`
- Margin: center

**Картка AI-відповіді**
- Background: `rgba(99, 102, 241, 0.08)`
- Border: `1px solid rgba(99, 102, 241, 0.3)`
- Border-radius: `16px`
- Padding: `20px`
- Shadow: `0 4px 16px rgba(99, 102, 241, 0.15)`

**Картка фіналу (success)**
- Background: `rgba(16, 185, 129, 0.08)`
- Border: `1px solid rgba(16, 185, 129, 0.3)`
- Border-radius: `20px`
- Padding: `32px`
- Icon: `✅` великий (48px)
- Text-align: center

### 3.3 Інпути (Inputs/Textarea)

**Text input**
- Background: `rgba(255, 255, 255, 0.05)`
- Border: `1px solid rgba(255, 255, 255, 0.1)`
- Border-radius: `12px`
- Padding: `14px 16px`
- Font: `16px / 400`
- Color: `#F8FAFC`
- Placeholder color: `#64748B`
- Focus: border `rgba(99, 102, 241, 0.6)`, ring `0 0 0 3px rgba(99, 102, 241, 0.15)`
- Transition: `all 0.2s ease`

**Textarea (для питання AI)**
- Min-height: `120px`
- Resize: vertical
- Інші стилі — як у text input

**Checkbox (згода на обробку даних)**
- Size: `20px`
- Background unchecked: `rgba(255, 255, 255, 0.05)`
- Border: `1px solid rgba(255, 255, 255, 0.2)`
- Border-radius: `4px`
- Background checked: gradient `linear-gradient(135deg, #6366F1, #8B5CF6)`
- Checkmark: білий `✓`
- Hover: border яскравіший

### 3.4 Чіпси (Chips) — швидкі підказки для AI

- Background: `rgba(255, 255, 255, 0.05)`
- Border: `1px solid rgba(255, 255, 255, 0.1)`
- Border-radius: `20px`
- Padding: `10px 16px`
- Font: `14px / 500`
- Color: `#CBD5E1`
- Hover: border `rgba(99, 102, 241, 0.4)`, background `rgba(99, 102, 241, 0.08)`
- Transition: `all 0.15s ease`

### 3.5 Прогрес-бар

**Position**: sticky top, full width
- Background: `rgba(15, 23, 42, 0.95)` з backdrop-blur `8px`
- Height: `8px` (сама лінія)
- Container padding: `16px 0`
- Border-bottom: `1px solid rgba(255, 255, 255, 0.05)`

**Лінія прогресу**
- Background (empty): `rgba(255, 255, 255, 0.08)`
- Background (filled): gradient `linear-gradient(90deg, #6366F1, #8B5CF6)`
- Border-radius: `4px`
- Transition: `width 0.3s ease`
- Box-shadow: `0 0 12px rgba(99, 102, 241, 0.4)` (на заповненій частині)

**Текст під прогресом**
- Font: `13px / 500`
- Color: `#94A3B8`
- Text: "Крок X з 7" або відсоток
- Position: справа від лінії або під нею (mobile)

### 3.6 Степпер-бейджі (номери кроків)

**Неактивний крок**
- Background: `rgba(255, 255, 255, 0.05)`
- Size: `32px` (кружок)
- Border: `1px solid rgba(255, 255, 255, 0.1)`
- Font: `14px / 600`
- Color: `#64748B`

**Активний крок**
- Background: gradient `linear-gradient(135deg, #6366F1, #8B5CF6)`
- Size: `32px`
- Border: none
- Font: `14px / 600`
- Color: `#FFFFFF`
- Shadow: `0 0 16px rgba(99, 102, 241, 0.5)`

**Завершений крок**
- Background: `#10B981`
- Icon: `✓`
- Color: `#FFFFFF`

---

## 4. LAYOUT (Компонування)

### Container
- Max-width: `680px` (для контенту)
- Padding: `24px` (desktop), `16px` (mobile)
- Margin: `0 auto`

### Відступи секцій
- Між заголовком і контентом: `24px` (desktop), `20px` (mobile)
- Між кнопками вибору: `12px`
- Між різними елементами форми: `16px`
- Padding картки від краю екрана (mobile): `16px`

### Grid для кнопок вибору
- Desktop: `grid-template-columns: repeat(2, 1fr)`
- Mobile: `grid-template-columns: 1fr`
- Gap: `12px`

### Breakpoints
- **Mobile**: `< 640px`
  - Одна колонка для всіх кнопок
  - Padding карток `24px → 20px`
  - Font sizes зменшуються (див. Typography)
  - Кнопки full-width
- **Tablet**: `640px – 1024px`
  - Дві колонки для кнопок вибору
  - Container width `640px`
- **Desktop**: `> 1024px`
  - Max container `680px`
  - Всі елементи на повну ширину контейнера

### Vertical rhythm
- Відстань між картками (якщо кілька): `32px`
- Padding верх/низ сторінки: `40px` (desktop), `24px` (mobile)

---

## 5. EFFECTS (Ефекти)

### Тіні (Shadows)

**Картки**
- Default: `0 8px 32px rgba(0, 0, 0, 0.3)`
- Hover (якщо інтерактивна): `0 12px 40px rgba(0, 0, 0, 0.4)`

**Кнопки primary**
- Default: `0 8px 24px rgba(99, 102, 241, 0.25)`
- Hover: `0 12px 32px rgba(99, 102, 241, 0.35)`

**Кнопки вибору (active)**
- `0 4px 16px rgba(99, 102, 241, 0.2)`

**Інпути focus**
- Ring: `0 0 0 3px rgba(99, 102, 241, 0.15)`

### Градієнти

**Primary button**
```
linear-gradient(135deg, #6366F1 0%, #8B5CF6 100%)
```

**Background accent (опційно для декору)**
```
radial-gradient(circle at top right, rgba(139, 92, 246, 0.15), transparent 50%)
radial-gradient(circle at bottom left, rgba(99, 102, 241, 0.1), transparent 50%)
```
Розмістити як псевдоелементи `::before` / `::after` на body або wrapper.

**Прогрес-бар filled**
```
linear-gradient(90deg, #6366F1 0%, #8B5CF6 100%)
```

### Blur & Glass (скло)

**Backdrop blur для карток**
- `backdrop-filter: blur(12px)`
- Працює тільки з напівпрозорим фоном (`rgba`)

**Glass effect для прогрес-бару (sticky header)**
- `backdrop-filter: blur(8px)`
- Background: `rgba(15, 23, 42, 0.95)`

### Анімації

**Допустимі анімації (легкі, не відволікають)**

1. **Fade-in при вході на крок**
   - Opacity `0 → 1`
   - Transform `translateY(10px) → translateY(0)`
   - Duration: `0.3s ease-out`

2. **Button hover scale**
   - Transform: `scale(1.02)`
   - Duration: `0.2s ease`

3. **Progress bar fill**
   - Width transition: `0.3s ease`

4. **Pulse на іконці (необов'язково)**
   - Анімація пульсації на `✅` у фінальному екрані
   - Keyframes: `scale(1) → scale(1.05) → scale(1)`
   - Duration: `1.5s infinite ease-in-out`

5. **Skeleton loader для AI (поки думає)**
   - Shimmer animation (світлова хвиля)
   - Background: `linear-gradient(90deg, transparent, rgba(255,255,255,0.05), transparent)`
   - Animation: `shimmer 1.5s infinite`

**НЕ використовувати:**
- Складні 3D transforms
- Rotate більше 5°
- Bounce effects
- Слайдери з автоплеєм

---

## 6. QA (Контроль якості)

### Контраст (WCAG AA)

**Перевірити:**
- Білий текст (`#F8FAFC`) на темному фоні (`#0F172A`): ✅ контраст > 7:1
- Сірий підпис (`#64748B`) на темному фоні: контраст > 4.5:1 (для body text)
- Primary button текст (`#FFFFFF`) на градієнті (`#6366F1`): ✅ > 4.5:1

**Інструменти:**
- WebAIM Contrast Checker
- Chrome DevTools → Lighthouse → Accessibility

### Читабельність на мобільних

- Мінімальний розмір тексту: `15px` (iOS Safari масштабує менше автоматично)
- Мінімальний розмір кнопки (tap target): `48px` висота
- Line-height для body text: `1.6` (для зручного читання на маленьких екранах)
- Відступи між інтерактивними елементами: мінімум `12px`

### Touch targets (мобільні)

- Кнопки вибору: `min-height: 64px`
- Primary/secondary buttons: `min-height: 48px`
- Checkbox: `20x20px` + padding навколо лейбла
- Chip buttons: `min-height: 40px`

### Performance

- Використовувати `will-change: transform` на кнопках з анімаціями (обережно, тільки на hover)
- Backdrop-blur може сповільнювати на слабких пристроях — тестувати на реальних телефонах
- Lazy load для heavy контенту (якщо будуть зображення)

---

## 7. УДОСКОНАЛЕННЯ UI/UX (топ-5)

### 1. **Мікроанімації підтвердження вибору**
**Що:** Коли користувач натискає на кнопку вибору (бізнес/канал/біль), показувати короткий feedback:
- Іконка `✅` з'являється з легкою анімацією `scale(0.8 → 1.1 → 1)` + зелений відблиск
- Вібрація (якщо мобільний) через Haptic API
- Плавний перехід на наступний крок через `0.4s` (щоб користувач побачив, що вибір зафіксовано)

**Навіщо:** Миттєвий візуальний feedback знижує тривогу "чи натиснулось?" і робить процес приємнішим.

---

### 2. **Skeleton loader з personality для AI-кроку**
**Що:** Поки AI "думає", показувати не просто спіннер, а:
- Блок з shimmer-анімацією (світлова хвиля)
- Текст: "Система аналізує твій бізнес…" (анімація typing effect або dots `...`)
- Іконка 🤖 або ⚙️ з легкою пульсацією

**Навіщо:** Довге очікування (1-3 сек) відчувається коротшим, якщо є візуальна індикація прогресу. Personality (текст + іконка) знижує напругу.

---

### 3. **Резюме вибору перед фіналом (на кроці "Контакт")**
**Що:** Під полями імені та контакту показати компактну картку з резюме:
```
Твій бізнес: Салон / барбершоп
Канали: Instagram / Facebook
Біль: Пишуть і пропадають
```
Кожен пункт — маленька іконка або бейдж, font `14px`, color `#CBD5E1`.

**Навіщо:** Користувач бачить, що система "запам'ятала" його відповіді → впевненість, що отримає релевантне рішення. Також можливість виправити (кнопка "← Назад" завжди доступна).

---

### 4. **Прогрес-бар з мікро-фідбеком**
**Що:** При переході між кроками:
- Лінія прогресу заповнюється плавно (не миттєво)
- Коротка вібрація або звук "клік" (опційно, тільки якщо користувач увімкнув)
- Текст "Крок X з 7" змінюється з fade-in/fade-out

**Навіщо:** Людина завжди знає, скільки залишилось. Відсутність здогадок = менше стресу.

---

### 5. **Фінальний екран з next action (не тільки "дякуємо")**
**Що:** Після відправки заявки показати:
- ✅ великий (48px)
- Заголовок: "Заявка зафіксована"
- Підзаголовок: "Якщо рішення підходить — наступний крок — реалізація."
- **Додатково:** 2 кнопки:
  - "Повернутися на головну" (secondary)
  - "Переглянути інші рішення" (ghost) → якщо є ще контент
- Опційно: таймер "Ми відповімо протягом 2 годин" з countdown (тільки якщо це реально)

**Навіщо:** Фінал — не тупик. Користувач розуміє, що далі, і може продовжити взаємодію (не йде одразу).

---

## ПІДСУМОК

Цей дизайн-гайд створює:
- **Швидкий** візуальний досвід (легкі анімації, чіткі переходи)
- **Зрозумілий** інтерфейс (великі кнопки, контрастні кольори, читабельний текст)
- **Інтерактивний** флоу (мікроанімації, feedback, skeleton loaders)
- **Премиальний** вигляд (gradient buttons, glass cards, м'які тіні)
- **Mobile-first** підхід (touch targets 48px+, адаптивна типографіка)

Всі елементи протестовані на контраст (WCAG AA), адаптовані під мобільні та планшети, і створюють відчуття "це працює швидко і про мене".

---

**Файл створено:** для передачі GPT або іншому розробнику для імплементації.
**Формат:** текстовий markdown, без коду — тільки специфікації.

