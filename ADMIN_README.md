# 🔐 Админка и Форма заявок

## Что добавлено

### 1. Форма заявок на главной странице
- Расположена в секции "Контакты" (`#contact`)
- Поля: **Имя** + **Телефон**
- CTA: "✅ Бесплатная консультация • ⚡ Ответ за 24 часа"
- Мультиязычная (RU/UA/EN)
- После отправки показывает success-сообщение

### 2. Админка `/admin`
- URL: `https://temoweb.eu/admin`
- Защищена паролем
- Показывает все заявки в таблице
- Сортировка: новые сверху

### 3. API для заявок
- `POST /api/leads` - добавить заявку
- `GET /api/leads` - получить все заявки (требует пароль)

---

## Настройка на сервере

### 1. Создай файл `.env.local` на сервере:
```bash
cd /var/www/mujsajt
nano .env.local
```

### 2. Добавь пароль:
```env
ADMIN_PASSWORD=твой_сильный_пароль_123
```
Сохрани: **Ctrl+O**, Enter, **Ctrl+X**

### 3. Деплой:
```bash
git pull
npm run build
pm2 restart mujsajt
```

---

## Как использовать

### Админка
1. Открой `https://temoweb.eu/admin`
2. Введи пароль из `.env.local`
3. Увидишь все заявки с датами и телефонами

### Форма на сайте
- Пользователи заполняют имя + телефон
- Жмут "Отправить заявку"
- Заявка сохраняется в файл `data/leads.json`
- Ты видишь её в админке

---

## Безопасность
✅ Админка защищена паролем  
✅ Пароль в `.env.local` (не коммитится в Git)  
✅ Данные заявок хранятся локально на сервере  
✅ Папка `data/` добавлена в `.gitignore`

---

## Структура файлов
```
/app
  /api/leads/route.ts     # API для сохранения/получения заявок
  /admin/page.tsx         # Страница админки
  page.tsx                # Главная (с формой LeadForm)
/data
  leads.json              # Хранилище заявок (создаётся автоматически)
```



